#ifndef __INSERTION_H_
#define __INSERTION_H_
#include <chrono>
#include <iostream>
#include <iomanip>
#include <fstream>

using namespace std;
class Insertion {

public:


	double insertionSort(double arr[], int size, int i);
	double runTime(double arr[], int size, int i);
	
};
#endif
